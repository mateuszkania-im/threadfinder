# App environment. Controls dev-only tooling (e.g. the Design system switcher).
# Omit for development behaviour; set to "production" to hide dev tools.
VITE_APP_ENV=development

# Point at a backend to go live. Unset = in-memory mock data (default).
# VITE_API_BASE_URL=https://api.example.com
